# 传感器自动测试内容清单

**文档版本**: v1.1
**编写日期**: 2026-06-11
**适用固件**: develop_CenterCtrl 分支
**测试目标**: 验证传感器模块的异常处理、数据采集、配置热更新和平均值计算等功能

---

## 一、测试概述

### 1.0 评审结论

本文档的测试方向基本合理，已经覆盖传感器模块 P1 阶段最核心的功能面：正常抄读、通信异常、数值不变异常、历史数据回退、配置热更新、平均值计算和综合并发场景。

但从自动化闭环角度看，原清单仍偏向人工或物理环境验证，例如拔线、温箱、标准仪器、调试串口监听等。P1 阶段建议补齐“传感器模拟器 + 场景期望表 + 自动断言”的工程化描述，让测试系统能够预先控制模拟器返回值，再读取环控器处理结果并自动判断通过/失败。

配套文档分工如下：

| 文档 | 定位 | 说明 |
| :--- | :--- | :--- |
| 本文档 | 测什么 | 定义 P1 自动化测试项、优先级和验收范围 |
| `传感器测试场景与期望表.md` | 给什么输入、期望什么输出 | 定义每个场景的模拟器输入、环控器期望值、断言规则 |
| `传感器模拟器实现方案.md` | 怎么实现模拟器 | 定义 PC 端 RS485 从站模拟器的协议、接口和代码落点 |

### 1.1 测试范围

本文档涵盖传感器模块的自动化测试内容，包括：

- 传感器异常自动过滤温湿度
- 传感器异常使用历史数据
- 传感器正常抄读
- 传感器配置改变热更新（无需重启）
- 传感器计算平均值

### 1.2 关键阈值常量（测试基准）

| 参数 | 值 | 单位 | 说明 |
| :--- | :---: | :---: | :--- |
| `ErRead` 触发次数 | 10 | 次 | 连续通信失败触发异常 |
| `ErMax` 触发次数 | 100 | 次 | 连续数值不变触发异常 |
| `SET_ALARM_TIMEOUT` | 180 | 秒 | 告警恢复延迟 |
| `POLL_QUERY_SEC` | 6 | 步长 | 压差高优先级插入间隔 |
| 偏差剔除阈值 | ±10 | ℃ | 中位数偏差剔除范围 |

### 1.3 测试环境要求

```
硬件环境：
- 环控器主机 (GD32F407VG)
- RS485 传感器套件（温湿度/压差/CO2/NH3）
- 模拟传感器设备（用于模拟故障）
- 串口调试工具

软件环境：
- 固件版本: develop_CenterCtrl
- 调试工具: J-Link/ST-Link
- 串口监听工具: 用于采集通信数据
- PC 端测试系统: Node.js 后端 + 前端 ATE 页面
- 传感器模拟器: Node.js 串口服务，作为 RS485 从站响应环控器抄读
```

### 1.4 自动化闭环原则

每个自动化用例都应形成如下闭环：

1. 测试系统加载场景，明确本次模拟器应返回的传感器值或故障状态。
2. 测试系统调用传感器模拟器接口，写入影子寄存器或注入故障。
3. 环控器通过 RS485 主动发送抄读指令。
4. 模拟器按场景返回 Modbus RTU 数据，记录请求帧、响应帧、解析值和时间戳。
5. 测试系统读取环控器侧结果，例如 ATE TCP、调试寄存器、串口日志或导出的状态接口。
6. 断言引擎对比“模拟器预置值/故障”与“环控器处理结果”，生成测试报告。

因此，P1 阶段不能只维护一份测试项清单，还必须同步维护可执行的场景/期望表，避免测试时临时人工判断。

---

## 二、传感器异常自动过滤温湿度测试

### 2.1 通信失败异常过滤

#### 测试项：T-ABNF-001 传感器离线检测

```
测试目的：验证传感器通信失败达到阈值时自动过滤该传感器数据
前置条件：
  1. 系统正常运行，所有传感器在线
  2. 温度传感器正常采集（如第1路温度值25.0℃）
测试步骤：
  1. 拔掉第1路温度传感器通信线（模拟通信失败）
  2. 等待 10 次轮询失败（约 10 秒）
  3. 读取 Sensor_Data.Indoor_Temp[0] 的值
  4. 检查 Data_error_Counter[0] 计数器
  5. 检查 Sensor_Data._deployment_s 中第1路在线位
预期结果：
  1. Data_error_Counter[0] 从 0 递增到 10
  2. 第10次失败后，Data_error_Counter[0] 清零
  3. Sensor_Data.Indoor_Temp[0] 被设置为 INVALID_VALUE
  4. Sensor_Data._deployment_s 第1路在线位被清除
  5. 产生 ErRead 告警标志
自动化实现：
  1. 使用继电器模拟器控制传感器通信线通断
  2. 通过调试串口监听 Data_error_Counter 变化
  3. 验证 Sensor_Data 结构体中的数据值和状态位
```

#### 测试项：T-ABNF-002 数值不变异常过滤

```
测试目的：验证传感器读数长期不变时触发异常告警
前置条件：
  1. 系统正常运行，传感器在线
  2. 温度传感器读数正常变化
测试步骤：
  1. 使用模拟传感器固定返回值（如恒定 25.0℃）
  2. 等待 100 次轮询（约 100 秒）
  3. 检查 Data_Invariant_Counter 计数器
  4. 检查 ErMax 告警标志
预期结果：
  1. Data_Invariant_Counter 从 0 递增到 100
  2. 第100次不变后触发 ErMax 告警
  3. 告警信息包含传感器编号
自动化实现：
  1. 配置模拟传感器固定输出值
  2. 监控 Data_Invariant_Counter 变化
  3. 验证告警触发和上报
```

#### 测试项：T-ABNF-003 异常传感器数据剔除

```
测试目的：验证偏差剔除算法能自动过滤异常传感器
前置条件：
  1. 系统正常运行，部署 16 路室内温度传感器
  2. 大部分传感器正常读数（约 25℃）
测试步骤：
  1. 模拟第 5 路传感器返回异常值（如 50℃，超出中位数±10℃范围）
  2. 等待数据采集周期完成
  3. 检查 indoorth_deviation_check() 执行结果
  4. 验证第 5 路传感器是否被剔除
  5. 检查 ActualTemp 计算是否排除第 5 路
预期结果：
  1. 第 5 路传感器被标记为异常
  2. ActualTemp 计算不包含第 5 路数据
  3. 其他正常传感器数据不受影响
自动化实现：
  1. 配置多路模拟传感器
  2. 仅设置其中一路返回异常值
  3. 读取 ActualTemp 验证剔除效果
```

---

## 三、传感器异常使用历史数据测试

### 3.1 历史数据回退机制

#### 测试项：T-HIST-001 单路传感器失效历史回退

```
测试目的：验证单路传感器失效后使用历史数据回退
前置条件：
  1. 系统正常运行，传感器在线
  2. App_Save.sensor_history 中有有效历史温湿度
测试步骤：
  1. 记录当前 Sensor_Data.Indoor_Temp[0] 值（如 25.0℃）
  2. 拔掉第 1 路温度传感器
  3. 等待 ErRead 触发（10 次失败）
  4. 检查系统是否从 sensor_history 读取历史值
  5. 验证 Sensor_Data.Indoor_Temp[0] 是否使用历史值
预期结果：
  1. 第 1 路传感器数据使用 sensor_history 中的历史值
  2. 其他传感器数据正常更新
  3. ActualTemp 计算包含历史值
自动化实现：
  1. 预设 sensor_history 中的历史值
  2. 模拟传感器失效
  3. 验证数据回退逻辑
```

#### 测试项：T-HIST-002 全传感器失效历史回退

```
测试目的：验证所有传感器失效时从历史数据回退
前置条件：
  1. 系统正常运行，部署多路传感器
  2. App_Save.sensor_history 中有有效历史温湿度
测试步骤：
  1. 记录当前所有传感器值
  2. 断开所有温度传感器通信
  3. 等待所有传感器 ErRead 触发
  4. 检查 ActualTemp 和 ActualHumi 计算
  5. 验证是否从 sensor_history 回退
预期结果：
  1. 所有传感器数据被置为 INVALID_VALUE
  2. ActualTemp/ActualHumi 使用 sensor_history 中的历史值
  3. 系统继续运行，不崩溃
自动化实现：
  1. 断开所有传感器模拟连接
  2. 监控 ActualTemp/ActualHumi 来源
  3. 验证历史数据回退逻辑
```

#### 测试项：T-HIST-003 历史数据更新机制

```
测试目的：验证传感器正常时历史数据持续更新
前置条件：
  1. 系统正常运行，传感器在线
测试步骤：
  1. 记录初始 sensor_history 值
  2. 改变环境温度（如从 25℃ 升至 30℃）
  3. 等待数据稳定（约 30 秒）
  4. 读取 sensor_history 中的值
  5. 验证历史值是否更新为新温度
预期结果：
  1. sensor_history 中的值随实际温度更新
  2. 更新频率与采集周期一致
  3. 历史值与实际值一致
自动化实现：
  1. 使用温箱控制环境温度
  2. 定时读取 sensor_history
  3. 验证历史值更新逻辑
```

---

## 四、传感器正常抄读测试

### 4.1 温湿度传感器抄读

#### 测试项：T-READ-001 室内温度传感器抄读

```
测试目的：验证室内温度传感器数据采集正确性
前置条件：
  1. 系统正常运行，16 路室内温度传感器已部署
  2. 标准温度计已校准
测试步骤：
  1. 在标准温度环境（25.0±0.5℃）下
  2. 读取所有 16 路室内温度值
  3. 使用标准温度计测量每个位置实际温度
  4. 计算每路传感器误差
  5. 统计平均误差和最大误差
预期结果：
  1. 每路传感器精度 ±0.5℃
  2. 平均误差 < 0.3℃
  3. 无明显异常值
自动化实现：
  1. 使用标准温度源作为参考
  2. 通过调试串口批量读取温度值
  3. 自动计算误差统计
```

#### 测试项：T-READ-002 室内湿度传感器抄读

```
测试目的：验证室内湿度传感器数据采集正确性
前置条件：
  1. 系统正常运行，16 路室内湿度传感器已部署
  2. 标准湿度计已校准
测试步骤：
  1. 在标准湿度环境（50±5%RH）下
  2. 读取所有 16 路室内湿度值
  3. 使用标准湿度计测量每个位置实际湿度
  4. 计算每路传感器误差
  5. 统计平均误差和最大误差
预期结果：
  1. 每路传感器精度 ±3%RH
  2. 平均误差 < 2%RH
  3. 无明显异常值
自动化实现：
  1. 使用恒湿箱控制环境湿度
  2. 通过调试串口批量读取湿度值
  3. 自动计算误差统计
```

#### 测试项：T-READ-003 压差传感器抄读

```
测试目的：验证压差传感器数据采集正确性
前置条件：
  1. 系统正常运行，压差传感器已部署
  2. 标准压差计已校准
测试步骤：
  1. 在无风环境下读取 4 路室内压差值
  2. 使用标准压差计测量实际值
  3. 开启风机改变压差
  4. 再次读取并对比
预期结果：
  1. 每路传感器精度 ±2Pa
  2. 无风环境下压差接近 0
  3. 风机运行时压差正确反映
自动化实现：
  1. 使用压差校准源
  2. 通过调试串口读取压差值
  3. 自动验证精度
```

#### 测试项：T-READ-004 CO2 传感器抄读

```
测试目的：验证 CO2 传感器数据采集正确性
前置条件：
  1. 系统正常运行，CO2 传感器已部署
  2. 标准 CO2 检测仪已校准
测试步骤：
  1. 在标准环境（400ppm CO2）下读取数据
  2. 使用干冰或 CO2 气瓶增加浓度
  3. 再次读取并对比
预期结果：
  1. 精度 ±50ppm
  2. 浓度增加时传感器响应
  3. 数据合理
自动化实现：
  1. 使用 CO2 标准气体
  2. 通过调试串口读取 CO2 值
  3. 自动验证精度
```

### 4.2 数据采集时序测试

#### 测试项：T-READ-005 轮询队列调度

```
测试目的：验证传感器轮询队列按优先级调度
前置条件：
  1. 系统正常运行，所有传感器已连接
测试步骤：
  1. 通过调试串口查看传感器轮询日志
  2. 检查轮询顺序是否符合优先级配置
  3. 统计各传感器的轮询频率
  4. 验证无传感器被遗漏
预期结果：
  1. 轮询顺序符合优先级配置
  2. 压差传感器按 POLL_QUERY_SEC 间隔插入
  3. 所有传感器都被轮询到
自动化实现：
  1. 监听串口日志
  2. 解析轮询序列
  3. 验证调度逻辑
```

#### 测试项：T-READ-006 轮询周期验证

```
测试目的：验证传感器轮询周期符合配置
前置条件：
  1. 系统正常运行
测试步骤：
  1. 记录轮询周期配置值（如 1 秒）
  2. 使用调试工具记录实际轮询时间
  3. 统计 10 次轮询的实际间隔
  4. 计算平均值和标准差
预期结果：
  1. 实际轮询周期符合配置值
  2. 周期波动 < 10%
  3. 无遗漏轮询
自动化实现：
  1. 监听串口日志时间戳
  2. 自动计算轮询间隔
  3. 验证周期稳定性
```

---

## 五、传感器配置改变热更新测试

### 5.1 部署位配置热更新

#### 测试项：T-HOT-001 传感器启用热更新

```
测试目的：验证传感器启用配置无需重启立即生效
前置条件：
  1. 系统正常运行
  2. 第 3 路温度传感器当前禁用
测试步骤：
  1. 通过 HMI 启用第 3 路温度传感器
  2. 保存配置到 App_Save.sensor_setup.temp
  3. 等待轮询队列重建（约 1-2 秒）
  4. 读取 Sensor_Data.Indoor_Temp[2] 的值
  5. 检查 Sensor_Data._deployment_s 第 3 路在线位
预期结果：
  1. 配置保存成功
  2. 轮询队列自动重建
  3. 第 3 路传感器数据开始采集
  4. 在线位被正确设置
自动化实现：
  1. 通过串口发送配置修改命令
  2. 监控 make_poll_queue() 执行
  3. 验证新传感器数据采集
```

#### 测试项：T-HOT-002 传感器禁用热更新

```
测试目的：验证传感器禁用配置无需重启立即生效
前置条件：
  1. 系统正常运行
  2. 第 2 路温度传感器当前启用
测试步骤：
  1. 通过 HMI 禁用第 2 路温度传感器
  2. 保存配置到 App_Save.sensor_setup.temp
  3. 等待轮询队列重建
  4. 读取 Sensor_Data.Indoor_Temp[1] 的值
  5. 检查 Sensor_Data._deployment_s 第 2 路在线位
预期结果：
  1. 配置保存成功
  2. 轮询队列自动重建
  3. 第 2 路传感器停止采集
  4. 在线位被清除
自动化实现：
  1. 通过串口发送配置修改命令
  2. 监控轮询队列变化
  3. 验证传感器停止采集
```

#### 测试项：T-HOT-003 RS485 端口切换热更新

```
测试目的：验证 RS485 端口配置切换无需重启立即生效
前置条件：
  1. 系统正常运行
  2. 当前使用端口 6
测试步骤：
  1. 通过 HMI 修改 rs485port 从 6 到 7
  2. 保存配置
  3. 等待 Modbus 重建（约 2-3 秒）
  4. 读取传感器数据
  5. 验证新端口通信正常
预期结果：
  1. 旧 Modbus 连接关闭
  2. 新端口 Modbus 重建
  3. 传感器数据正常采集
  4. 无通信错误
自动化实现：
  1. 通过串口发送端口切换命令
  2. 监控 Modbus 重建日志
  3. 验证新端口通信
```

### 5.2 告警阈值配置热更新

#### 测试项：T-HOT-004 温度告警阈值热更新

```
测试目的：验证温度告警阈值配置无需重启立即生效
前置条件：
  1. 系统正常运行
  2. 当前温度 25℃，高限阈值 35℃
测试步骤：
  1. 通过 HMI 修改温度高限阈值从 35℃ 到 28℃
  2. 保存配置到 App_Save.alarm.TempHigh
  3. 等待 Alarm_Check() 周期执行
  4. 验证新阈值是否生效
  5. 检查是否触发温度高限告警
预期结果：
  1. 配置保存成功
  2. Alarm_Check() 使用新阈值
  3. 25℃ > 28℃ 条件不满足，无告警
  4. 若温度升高超过 28℃，立即触发告警
自动化实现：
  1. 通过串口修改告警阈值
  2. 监控 Alarm_Check() 执行
  3. 验证告警触发条件
```

#### 测试项：T-HOT-005 湿度告警阈值热更新

```
测试目的：验证湿度告警阈值配置无需重启立即生效
前置条件：
  1. 系统正常运行
  2. 当前湿度 50%RH，高限阈值 80%RH
测试步骤：
  1. 通过 HMI 修改湿度高限阈值从 80%RH 到 55%RH
  2. 保存配置到 App_Save.alarm.HumiHigh
  3. 等待 Alarm_Check() 周期执行
  4. 验证新阈值是否生效
  5. 检查是否触发湿度高限告警
预期结果：
  1. 配置保存成功
  2. Alarm_Check() 使用新阈值
  3. 50%RH < 55%RH 条件不满足，无告警
  4. 若湿度升高超过 55%，立即触发告警
自动化实现：
  1. 通过串口修改告警阈值
  2. 监控 Alarm_Check() 执行
  3. 验证告警触发条件
```

### 5.3 补偿参数配置热更新

#### 测试项：T-HOT-006 温度补偿值热更新

```
测试目的：验证温度补偿值配置无需重启立即生效
前置条件：
  1. 系统正常运行
  2. 第 1 路温度传感器补偿值为 0℃
测试步骤：
  1. 通过 HMI 设置第 1 路温度补偿值为 +1.5℃
  2. 保存配置到 App_Save.comp_th.TempDiff[0]
  3. 等待下一次传感器采集（约 1 秒）
  4. 读取 Sensor_Data.Indoor_Temp[0] 的值
  5. 验证补偿值是否生效
预期结果：
  1. 配置保存成功
  2. Indoor_Temp[0] = 原始值 + 1.5℃
  3. 补偿值立即生效
自动化实现：
  1. 通过串口修改补偿值
  2. 读取传感器原始值和补偿后值
  3. 验证补偿计算正确
```

#### 测试项：T-HOT-007 湿度补偿值热更新

```
测试目的：验证湿度补偿值配置无需重启立即生效
前置条件：
  1. 系统正常运行
  2. 第 1 路湿度传感器补偿值为 0%RH
测试步骤：
  1. 通过 HMI 设置第 1 路湿度补偿值为 -2.0%RH
  2. 保存配置到 App_Save.comp_th.HumiDiff[0]
  3. 等待下一次传感器采集
  4. 读取 Sensor_Data.Indoor_Humi[0] 的值
  5. 验证补偿值是否生效
预期结果：
  1. 配置保存成功
  2. Indoor_Humi[0] = 原始值 - 2.0%RH
  3. 补偿值立即生效
自动化实现：
  1. 通过串口修改补偿值
  2. 读取传感器原始值和补偿后值
  3. 验证补偿计算正确
```

---

## 六、传感器计算平均值测试

### 6.1 室内温度平均值计算

#### 测试项：T-AVG-001 正常多路温度平均值

```
测试目的：验证多路温度传感器平均值计算正确
前置条件：
  1. 系统正常运行，16 路温度传感器全部在线
  2. 各传感器读数已知
测试步骤：
  1. 设置 16 路温度传感器返回值：
     - 第 1-8 路：25.0℃
     - 第 9-16 路：26.0℃
  2. 等待数据采集稳定（约 5 秒）
  3. 读取 Sensor_Data.ActualTemp 的值
  4. 手动计算期望平均值：(25.0×8 + 26.0×8) / 16 = 25.5℃
  5. 验证 ActualTemp 是否等于 25.5℃
预期结果：
  1. ActualTemp = 25.5℃（±0.1℃ 误差）
  2. 计算排除异常传感器
  3. 无除零错误
自动化实现：
  1. 配置多路模拟传感器返回特定值
  2. 读取 ActualTemp
  3. 自动计算并验证平均值
```

#### 测试项：T-AVG-002 部分传感器失效平均值

```
测试目的：验证部分传感器失效时平均值计算正确
前置条件：
  1. 系统正常运行，16 路温度传感器部署
  2. 4 路传感器正常（25.0℃），12 路传感器失效
测试步骤：
  1. 模拟 12 路传感器通信失败
  2. 等待 ErRead 触发
  3. 读取 Sensor_Data.ActualTemp 的值
  4. 验证平均值仅计算有效传感器
预期结果：
  1. ActualTemp = 25.0℃（仅有效传感器参与计算）
  2. 失效传感器不参与平均值计算
  3. 无除零错误
自动化实现：
  1. 模拟部分传感器失效
  2. 读取 ActualTemp
  3. 验证计算逻辑
```

#### 测试项：T-AVG-003 全部传感器失效回退

```
测试目的：验证全部传感器失效时使用历史数据回退
前置条件：
  1. 系统正常运行
  2. App_Save.sensor_history 有有效历史值（25.0℃）
测试步骤：
  1. 断开所有温度传感器
  2. 等待所有 ErRead 触发
  3. 读取 Sensor_Data.ActualTemp 的值
  4. 验证是否使用历史值
预期结果：
  1. ActualTemp = 25.0℃（历史值）
  2. 系统继续运行
  3. 不发生除零错误
自动化实现：
  1. 断开所有传感器模拟连接
  2. 预设历史值
  3. 验证回退逻辑
```

### 6.2 室内湿度平均值计算

#### 测试项：T-AVG-004 正常多路湿度平均值

```
测试目的：验证多路湿度传感器平均值计算正确
前置条件：
  1. 系统正常运行，16 路湿度传感器全部在线
  2. 各传感器读数已知
测试步骤：
  1. 设置 16 路湿度传感器返回值：
     - 第 1-8 路：50.0%RH
     - 第 9-16 路：55.0%RH
  2. 等待数据采集稳定
  3. 读取 Sensor_Data.ActualHumi 的值
  4. 手动计算期望平均值：(50.0×8 + 55.0×8) / 16 = 52.5%RH
  5. 验证 ActualHumi 是否等于 52.5%RH
预期结果：
  1. ActualHumi = 52.5%RH（±0.5%RH 误差）
  2. 计算排除异常传感器
自动化实现：
  1. 配置多路模拟传感器返回特定值
  2. 读取 ActualHumi
  3. 自动计算并验证平均值
```

### 6.3 偏差剔除后平均值计算

#### 测试项：T-AVG-005 偏差剔除后平均值

```
测试目的：验证偏差剔除后平均值计算正确
前置条件：
  1. 系统正常运行，16 路温度传感器全部在线
  2. 大部分传感器读数约 25.0℃
测试步骤：
  1. 模拟第 5 路传感器返回 50.0℃（超出中位数±10℃范围）
  2. 等待 indoorth_deviation_check() 执行
  3. 读取 Sensor_Data.ActualTemp 的值
  4. 验证第 5 路被剔除，平均值仅计算剩余 15 路
预期结果：
  1. 第 5 路传感器被标记为异常
  2. ActualTemp 仅计算 15 路正常传感器
  3. 平均值约 25.0℃
自动化实现：
  1. 配置一路异常传感器
  2. 读取 ActualTemp
  3. 验证剔除后平均值计算
```

---

## 七、综合场景测试

### 7.1 传感器异常恢复测试

#### 测试项：T-COMP-001 传感器离线后恢复

```
测试目的：验证传感器离线后恢复时数据正常采集
前置条件：
  1. 系统正常运行
  2. 第 1 路温度传感器当前离线
测试步骤：
  1. 恢复第 1 路传感器连接
  2. 等待数据采集周期
  3. 读取 Sensor_Data.Indoor_Temp[0] 的值
  4. 检查 Data_error_Counter 和在线位
预期结果：
  1. 第 1 路传感器数据恢复正常采集
  2. Data_error_Counter 清零
  3. 在线位重新设置
  4. ErRead 告警清除
自动化实现：
  1. 恢复传感器模拟连接
  2. 监控数据恢复
  3. 验证告警清除
```

### 7.2 多传感器同时异常测试

#### 测试项：T-COMP-002 多路传感器同时失效

```
测试目的：验证多路传感器同时失效时系统稳定性
前置条件：
  1. 系统正常运行，16 路温度传感器全部在线
测试步骤：
  1. 同时断开 8 路传感器（第 1、3、5、7、9、11、13、15 路）
  2. 等待所有 ErRead 触发
  3. 读取 Sensor_Data.ActualTemp 的值
  4. 验证剩余 8 路传感器数据正常
  5. 验证平均值计算正确
预期结果：
  1. 失效传感器数据置为 INVALID_VALUE
  2. 剩余传感器数据正常采集
  3. ActualTemp 仅计算 8 路有效传感器
  4. 系统继续运行，不崩溃
自动化实现：
  1. 同时控制多路传感器模拟断开
  2. 监控系统稳定性
  3. 验证数据处理逻辑
```

### 7.3 配置热更新与数据采集并发测试

#### 测试项：T-COMP-003 配置更新与采集并发

```
测试目的：验证配置更新与数据采集并发时无冲突
前置条件：
  1. 系统正常运行，传感器在线
测试步骤：
  1. 开始持续读取传感器数据
  2. 同时修改传感器部署位配置
  3. 监控数据采集是否中断
  4. 验证配置更新后数据正常
预期结果：
  1. 数据采集不中断
  2. 配置更新后轮询队列重建
  3. 新配置立即生效
  4. 无数据竞争或崩溃
自动化实现：
  1. 并发执行配置修改和数据读取
  2. 监控系统稳定性
  3. 验证数据一致性
```

---

## 八、测试用例汇总

| 测试类别 | 用例数量 | 关键验证点 |
| :--- | :---: | :--- |
| 异常自动过滤 | 5 | ErRead/ErMax 触发、数据剔除、告警 |
| 历史数据回退 | 3 | 单路/全路失效回退、历史更新 |
| 正常抄读 | 6 | 温湿度/压差/CO2 精度、轮询调度 |
| 配置热更新 | 7 | 部署位/阈值/补偿值/端口切换 |
| 平均值计算 | 5 | 正常/部分失效/全部失效/偏差剔除 |
| 综合场景 | 3 | 异常恢复、多路同时异常、并发安全 |
| **总计** | **29** | - |

---

## 九、自动化测试框架建议

### 9.1 测试架构

```
┌─────────────────────────────────────────────────────────┐
│                    测试管理层                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │  测试用例   │  │  测试数据   │  │  测试报告   │      │
│  │  管理       │  │  管理       │  │  生成       │      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    测试执行层                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │  串口通信   │  │  状态监控   │  │  断言验证   │      │
│  │  模块       │  │  模块       │  │  模块       │      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    硬件抽象层                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │  传感器     │  │  继电器     │  │  串口       │      │
│  │  模拟器     │  │  控制器     │  │  转换器     │      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
└─────────────────────────────────────────────────────────┘
```

### 9.2 关键测试函数示例

```c
// 传感器数据读取函数
rt_err_t test_sensor_read_data(uint8_t sensor_type, uint8_t index, float *value);

// 传感器配置修改函数
rt_err_t test_sensor_config_modify(uint8_t config_type, uint8_t index, uint32_t value);

// 传感器状态监控函数
rt_err_t test_sensor_status_monitor(uint8_t *online_status, uint32_t *error_counter);

// 断言验证函数
void test_assert_float_eq(float actual, float expected, float tolerance, const char *test_name);
void test_assert_bit_set(uint32_t bitmap, uint8_t bit_pos, const char *test_name);
void test_assert_bit_clear(uint32_t bitmap, uint8_t bit_pos, const char *test_name);
```

---

## 十、模拟器实现配套文档与接口要求

### 10.1 模拟器在 P1 自动化中的定位

传感器模拟器不是单独调试工具，而是自动化测试系统的受控输入源。它需要被 `TestManager` 或测试编排层直接调用，按测试场景动态改变传感器返回值、通信状态和异常行为。

建议正式实现放在当前工程后端：

| 模块 | 建议文件 | 责任 |
| :--- | :--- | :--- |
| 传感器模拟器 | `backend/ate/SensorSimulator.js` | 打开 USB-RS485 串口，解析环控器 Modbus RTU 请求，按影子寄存器回复 |
| 场景目录 | `backend/ate/TestScenarioCatalog.js` | 加载传感器场景和期望表，统一管理 scenarioId |
| 控制器读取 | `backend/ate/ControllerStateReader.js` | 从 ATE TCP、调试串口或寄存器读取环控器实际处理结果 |
| 断言引擎 | `backend/ate/AssertEngine.js` | 比较 expected 与 actual，输出通过/失败原因 |
| 测试编排 | `backend/ate/TestManager.js` | 串联 applyScenario、等待采集、读取结果、断言、报告 |

### 10.2 必须具备的模拟器能力

| 能力 | 说明 | 对应用例 |
| :--- | :--- | :--- |
| 多从站地址 | 同一 USB-RS485 口模拟多路温湿度、压差、CO2、NH3 等从站 | 正常抄读、多路异常 |
| 影子寄存器表 | 保存每个从站、寄存器地址、原始值、工程值、单位和缩放系数 | 所有闭环测试 |
| 动态改值 | 测试过程中实时调整温度、湿度、压差、气体浓度 | 环控逻辑、平均值、热更新 |
| 不响应/超时 | 指定从站或指定寄存器不回复 | ErRead、离线恢复 |
| 固定值不变 | 连续返回相同值 | ErMax 数值不变异常 |
| 异常跳变 | 返回明显偏离中位数或越界值 | 偏差剔除、异常过滤 |
| CRC 错误 | 故意返回错误 CRC | 通信异常鲁棒性 |
| 延迟响应 | 模拟总线慢响应 | 超时边界、轮询稳定性 |
| 交易日志 | 记录请求帧、响应帧、解析值、场景 ID、时间戳 | 问题定位、报告证据 |

### 10.3 模拟器接口契约

模拟器至少提供以下内部接口，供 `TestManager` 调用：

```js
await sensorSimulator.start({
  port: 'COMx',
  baudRate: 9600,
  parity: 'none',
  dataBits: 8,
  stopBits: 1
})

await sensorSimulator.stop()

await sensorSimulator.applyScenario({
  scenarioId: 'SEN-TEMP-001',
  sensors: [
    { key: 'temp_1', slaveAddr: 1, value: 20.0, unit: 'C', scale: 10 },
    { key: 'humi_1', slaveAddr: 1, value: 65.0, unit: '%RH', scale: 10 }
  ],
  faults: []
})

await sensorSimulator.setSensorValue('temp_1', 25.5)
await sensorSimulator.setRegister({ slaveAddr: 1, registerAddr: 0x0000, rawValue: 255 })
await sensorSimulator.injectTimeout({ key: 'temp_1' })
await sensorSimulator.injectFixedValue({ key: 'temp_1', value: 25.0, repeat: 100 })
await sensorSimulator.injectOutlier({ key: 'temp_1', value: 80.0 })
await sensorSimulator.injectCrcError({ key: 'temp_1', count: 3 })
await sensorSimulator.clearFault('temp_1')

const shadow = sensorSimulator.getShadowRegisters()
const log = sensorSimulator.getTransactionLog({ scenarioId: 'SEN-TEMP-001' })
await sensorSimulator.reset()
```

### 10.4 影子寄存器表要求

影子寄存器表是测试系统知道“模拟器返回了什么”的核心数据源。每个传感器点位至少应包含：

| 字段 | 示例 | 说明 |
| :--- | :--- | :--- |
| `key` | `temp_1` | 测试系统内部唯一名称 |
| `sensorType` | `temperature` | 传感器类型 |
| `slaveAddr` | `1` | Modbus RTU 从站地址 |
| `registerAddr` | `0x0000` | 数据寄存器地址 |
| `rawValue` | `200` | 实际回复的寄存器原始值 |
| `value` | `20.0` | 工程值 |
| `scale` | `10` | 工程值到寄存器值的缩放倍数 |
| `unit` | `C` | 单位 |
| `status` | `normal` | `normal/timeout/fixed/outlier/crcError` |
| `updatedAt` | `2026-06-11T10:00:00+08:00` | 最近更新时间 |

### 10.5 交易日志要求

每次环控器抄读都必须落交易日志，便于失败时定位是“环控器没发请求”“模拟器没回”“回了但环控器解析错”还是“断言规则错”。

```json
{
  "scenarioId": "SEN-TEMP-001",
  "timestamp": "2026-06-11T10:00:00.123+08:00",
  "direction": "request-response",
  "slaveAddr": 1,
  "functionCode": 3,
  "requestHex": "010300000001840A",
  "responseHex": "01030200C8B9D2",
  "registerAddr": 0,
  "rawValue": 200,
  "value": 20.0,
  "unit": "C",
  "fault": null,
  "result": "responded"
}
```

---

## 十一、测试用例与模拟器动作映射

| 测试类别 | 典型用例 | 模拟器动作 | 环控器侧断言 |
| :--- | :--- | :--- | :--- |
| 正常抄读 | 温湿度、压差、CO2 正常采集 | `setSensorValue` 设置稳定值 | 读取值与模拟器工程值一致，误差在容差内 |
| 通信失败 | T-ABNF-001 | `injectTimeout` 连续不响应 10 次 | `Data_error_Counter` 达阈值，数据置无效，在线位清除 |
| 数值不变 | T-ABNF-002 | `injectFixedValue` 连续返回同一值 100 次 | 触发 `ErMax`，该路数据被过滤 |
| 异常跳变 | 温度突变/偏差剔除 | `injectOutlier` 返回偏离中位数值 | 该值不参与平均值或触发异常处理 |
| 历史回退 | 单路或全路失效 | 先正常值，再 `injectTimeout` | 使用最近有效历史值，超出策略后进入无效状态 |
| 配置热更新 | 部署位、补偿值、地址变化 | `applyScenario` 切换点位表 | 轮询队列重建，新配置生效，无需重启 |
| 平均值计算 | 部分失效/全部失效 | 多点设置正常值、无效值、离群值 | `ActualTemp` 等平均值符合期望表 |
| 异常恢复 | 离线后恢复 | `clearFault` 后重新返回正常值 | 在线位恢复，告警按延时策略恢复 |
| 环控联动 | 温度上升触发加热/通风策略变化 | 分阶段 `setSensorValue` 梯度变化 | 执行器状态、告警状态、控制逻辑符合期望 |

---

## 十二、模拟器实现验收清单

P1 阶段建议把模拟器也纳入测试验收，至少验证以下内容：

| 验收项 | 验收标准 |
| :--- | :--- |
| 串口启动/关闭 | 能打开指定 COM 口，重复启动/关闭不残留句柄 |
| Modbus RTU CRC | 正确校验请求 CRC，正确生成响应 CRC |
| 功能码支持 | 至少支持环控器抄读使用的读保持寄存器/读输入寄存器功能码 |
| 多从站支持 | 同一串口可按从站地址返回不同传感器数据 |
| 多寄存器读取 | 支持一次读取多个连续寄存器 |
| 半包/粘包处理 | 串口分包、连续帧不导致解析错乱 |
| 超时注入 | 指定点位可不响应，且不影响其他点位 |
| 异常帧注入 | 可注入 CRC 错误、非法功能码或异常响应 |
| 日志完整性 | 每次请求/响应都有场景 ID、时间戳、十六进制帧和解析值 |
| 场景复位 | 每个用例结束后能清空故障、影子寄存器和交易日志 |

---

## 十三、建议首批落地顺序

为了降低 P1 实现风险，建议按以下顺序开发和验证：

1. 先实现 `SensorSimulator` 的正常 Modbus RTU 回复能力，只覆盖 1 路温度和 1 路湿度。
2. 接入 `TestManager.applyScenario`，让测试系统能够把场景值写入模拟器。
3. 实现交易日志，确保测试失败时能看到环控器请求和模拟器响应。
4. 跑通 `正常抄读 -> 读取环控器结果 -> 自动断言` 的最小闭环。
5. 再扩展通信超时、固定值不变、异常跳变、CRC 错误等故障注入能力。
6. 最后接入平均值、历史回退、热更新和环控逻辑联动场景。

首批建议落地场景：

| 优先级 | 场景 | 原因 |
| :---: | :--- | :--- |
| P0 | 单路温度正常抄读 | 验证模拟器、环控器、断言链路是否打通 |
| P0 | 单路温度通信失败 ErRead | 验证最关键的异常闭环 |
| P0 | 多路温度平均值 | 验证影子寄存器与期望表能支撑聚合计算 |
| P1 | 固定值不变 ErMax | 验证长周期异常注入 |
| P1 | 配置热更新后改地址/部署位 | 验证场景切换与轮询队列重建 |
| P1 | 温度阶跃触发环控逻辑 | 验证传感器测试向环控系统测试扩展 |

---

**文档结束**
